/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Java project to get you started.
 * For more details take a look at the Java Quickstart chapter in the Gradle
 * User Manual available at https://docs.gradle.org/5.2/userguide/tutorial_java_projects.html
 */

plugins {
    // Apply the java plugin to add support for Java
    id 'java'

    // Apply the application plugin to add support for building an application
    id 'application'

    // Apply code coverage
    id 'jacoco'
}

jacoco {
    toolVersion = "0.8.2"
    reportsDir = file("$buildDir/reports")
    applyTo run
}

task codeCoverageReport(type:JacocoReport) {
    executionData run
    sourceSets sourceSets.main
    reports {
        xml.enabled true
        xml.destination file("${buildDir}/reports/report.xml")
        html.enabled false
        csv.enabled false
    }
}

repositories {
    // Use jcenter for resolving your dependencies.
    // You can declare any Maven/Ivy/file repository here.
    jcenter()
}

sourceSets {
    main {
        java {
            srcDirs = ['src/src_java/main/java']
        }
        resources {
            srcDirs = ['src/src_java/main/resources']
        }
    }
}

// Define the main class for the application
mainClassName = 'hello_world.App'
